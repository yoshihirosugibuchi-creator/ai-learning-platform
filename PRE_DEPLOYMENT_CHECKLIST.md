# 🚀 デプロイ前完全チェックリスト - 絶対失敗防止版

**対象**: AI学習プラットフォーム本番デプロイ  
**目的**: デプロイ漏れ・デグレ・機能停止の完全防止  
**作成日**: 2025年10月14日  
**重要度**: ⭐⭐⭐⭐⭐ 最高レベル

---

## ⛔ **デプロイ禁止条件（一つでも該当する場合はデプロイ中止）**

- [ ] TypeScriptエラーが1個でも存在
- [ ] ESLintエラーが1個でも存在  
- [ ] ビルドが失敗
- [ ] XP/SKP計算システムのテストが失敗
- [ ] ナレッジカード機能のテストが失敗
- [ ] 認証システムのテストが失敗
- [ ] データベース整合性チェックが失敗

---

## 📋 **Phase 1: 基本品質チェック（必須・自動化可能）**

### **A. コード品質確認**
```bash
# 1. TypeScript型チェック
echo "🔍 TypeScript型チェック実行中..."
npm run typecheck
if [ $? -ne 0 ]; then
  echo "❌ TypeScriptエラーあり - デプロイ中止"
  exit 1
fi
echo "✅ TypeScriptエラー: 0個"

# 2. ESLintチェック  
echo "🔍 ESLintチェック実行中..."
npm run lint
if [ $? -ne 0 ]; then
  echo "❌ ESLintエラーあり - デプロイ中止"
  exit 1
fi
echo "✅ ESLintエラー: 0個"

# 3. ビルドテスト
echo "🔍 プロダクションビルド実行中..."
npm run build
if [ $? -ne 0 ]; then
  echo "❌ ビルド失敗 - デプロイ中止"
  exit 1
fi
echo "✅ ビルド成功"
```

### **B. 重要ファイル存在確認**
- [ ] `.env.local` 存在確認
- [ ] `lib/database-types-official.ts` 存在確認  
- [ ] `CLAUDE.md` 存在確認
- [ ] `package.json` 依存関係確認

---

## 🧪 **Phase 2: コア機能回帰テスト（手動実行必須）**

### **A. XP/SKP計算システム完全テスト**

#### **テストケース1: ランダムクイズ（難易度未指定）**
```markdown
操作手順:
1. [ ] ログイン後、トップページでランダムクイズを開始
2. [ ] 難易度を選択せずに「クイズ開始」
3. [ ] 10問中7-8問正解（70-80%の正解率で回答）
4. [ ] クイズ完了後の結果画面確認

期待結果:
- [ ] XP獲得量が表示される
- [ ] SKP獲得量が表示される
- [ ] プロフィール画面で統計が更新される
- [ ] `quiz_sessions` テーブルに記録される
- [ ] `user_xp_stats_v2` テーブルが更新される

確認コマンド:
-- 最新のクイズセッション確認
SELECT * FROM quiz_sessions WHERE user_id = 'ユーザーID' ORDER BY completed_at DESC LIMIT 1;
-- XP統計確認
SELECT total_xp, total_skp FROM user_xp_stats_v2 WHERE user_id = 'ユーザーID';
```

#### **テストケース2: カテゴリー指定クイズ（単一難易度）**
```markdown
操作手順:
1. [ ] 特定カテゴリー（例：ITスキル）を選択
2. [ ] 単一難易度（例：初級）を選択してクイズ開始
3. [ ] 10問中10問正解（100%正解でボーナス確認）
4. [ ] 結果画面での詳細確認

期待結果:
- [ ] 100%ボーナスが適用される
- [ ] カテゴリー別統計が更新される
- [ ] サブカテゴリー別統計が更新される

確認コマンド:
-- カテゴリー別統計確認
SELECT * FROM user_category_xp_stats_v2 WHERE user_id = 'ユーザーID' AND category_id = '選択カテゴリーID';
```

### **B. コース学習システム完全テスト**

#### **テストケース3: セッション完了→テーマ完了→ナレッジカード獲得**
```markdown
操作手順:
1. [ ] 3C分析コースの「So What思考法」を選択
2. [ ] 最初のセッションを完了
3. [ ] テーマ内の全セッションを順次完了
4. [ ] テーマ完了画面でナレッジカード獲得確認
5. [ ] コレクションページでカード表示確認

期待結果:
- [ ] セッション完了ごとにXP獲得
- [ ] テーマ完了時に「ナレッジカードを獲得！」表示
- [ ] コレクションページに新しいカードが表示
- [ ] カードのタイトル・説明が正しく表示

確認コマンド:
-- ナレッジカード確認
SELECT card_id, count, obtained_at FROM knowledge_card_collection WHERE user_id = 'ユーザーID' ORDER BY obtained_at DESC;
-- カードID変換確認（コンソールログで確認）
Math.abs('theme_so_what_why_so'.split('').reduce((a, b) => a + b.charCodeAt(0), 0))
```

#### **テストケース4: コース完了検証**
```markdown
操作手順:
1. [ ] コース内の全セッションを完了（前のテストの続きまたは新しいコース）
2. [ ] 最後のセッション完了時にコース完了画面確認
3. [ ] 修了証の表示確認
4. [ ] データベースでコース完了記録確認

期待結果:
- [ ] 「修了証を獲得！」表示
- [ ] `course_completions` テーブルに記録
- [ ] SKP取引履歴に記録

確認コマンド:
-- コース完了記録確認
SELECT * FROM course_completions WHERE user_id = 'ユーザーID' ORDER BY completed_at DESC LIMIT 1;
-- SKP取引確認
SELECT * FROM skp_transactions WHERE user_id = 'ユーザーID' AND transaction_type = 'course_completion' ORDER BY created_at DESC LIMIT 1;
```

### **C. ナレッジカードシステム完全テスト**

#### **テストケース5: カードID整合性確認**
```markdown
操作手順:
1. [ ] 開発者ツールのコンソールを開く
2. [ ] 任意のテーマを完了してカード獲得
3. [ ] コンソールログで生成されるカードIDを記録
4. [ ] コレクションページでカードが正しく表示されるか確認
5. [ ] `/api/debug/knowledge-cards` API直接呼び出しでデータ確認

期待結果:
- [ ] カード生成時のID（`theme_${themeId}`形式）が正しい
- [ ] データベース格納時の数値IDが正しい
- [ ] コレクション表示時のID変換が正しい
- [ ] カードの重複取得が正しく処理される

テスト用API呼び出し:
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:3000/api/debug/knowledge-cards
```

### **D. 認証・セキュリティシステムテスト**
```markdown
操作手順:
1. [ ] ログアウト→ログイン正常動作確認
2. [ ] 他ユーザーの学習データへのアクセス拒否確認
3. [ ] API認証エラー時の適切な処理確認
4. [ ] セッション切れ時のリダイレクト確認

期待結果:
- [ ] 認証が必要なページで適切にログイン画面に誘導
- [ ] RLS（Row Level Security）が正常動作
- [ ] エラーメッセージが適切に表示

確認方法:
-- 直接データベースクエリでRLS確認
SELECT * FROM user_xp_stats_v2 WHERE user_id != '自分のユーザーID';  -- エラーまたは0件であること
```

---

## 🗄️ **Phase 3: データベース整合性チェック（重要）**

### **A. テーブル間関係性確認**
```sql
-- 1. セッション記録とクイズ回答の整合性
SELECT 
  qs.id as session_id,
  qs.user_id,
  qs.completed_at,
  COUNT(qa.id) as answer_count
FROM quiz_sessions qs
LEFT JOIN quiz_answers qa ON qs.id = qa.session_id  
WHERE qs.completed_at >= NOW() - INTERVAL '1 day'
GROUP BY qs.id, qs.user_id, qs.completed_at
HAVING COUNT(qa.id) = 0;  -- 回答がないセッションがあるかチェック

-- 2. XP統計の一貫性
SELECT 
  user_id,
  total_xp,
  (SELECT SUM(earned_xp) FROM quiz_sessions WHERE user_id = user_xp_stats_v2.user_id) as calculated_xp
FROM user_xp_stats_v2
WHERE total_xp != (SELECT COALESCE(SUM(earned_xp), 0) FROM quiz_sessions WHERE user_id = user_xp_stats_v2.user_id);

-- 3. ナレッジカードの重複確認  
SELECT user_id, card_id, COUNT(*) as duplicate_count
FROM knowledge_card_collection
GROUP BY user_id, card_id
HAVING COUNT(*) > 1;
```

### **B. データベーススキーマ確認**
- [ ] `course_completions` テーブルから不要カラム（`completion_bonus_skp`）が削除済み
- [ ] 全必要カラムが存在する
- [ ] インデックスが適切に設定されている

---

## 🎯 **Phase 4: エンドユーザー体験確認**

### **A. レスポンシブ・UI/UX確認**
- [ ] スマートフォン表示での動作確認
- [ ] タブレット表示での動作確認
- [ ] ナビゲーション・画面遷移の自然さ確認
- [ ] ローディング状態の適切な表示確認

### **B. エラーハンドリング確認**
- [ ] ネットワークエラー時の表示確認
- [ ] APIタイムアウト時の処理確認  
- [ ] 不正なデータ入力時の処理確認
- [ ] サーバーエラー時のユーザー向けメッセージ確認

---

## ⚡ **Phase 5: パフォーマンス・本番環境確認**

### **A. ページ読み込み速度確認**
- [ ] トップページの初期読み込み（3秒以内）
- [ ] クイズページの読み込み（2秒以内）
- [ ] コレクションページの読み込み（2秒以内）
- [ ] プロフィールページの読み込み（2秒以内）

### **B. データベースパフォーマンス確認**
```sql
-- 重いクエリの確認（実行時間が1秒以上のものをチェック）
-- 本番環境での確認が必要
```

### **C. 本番環境での動作確認**
- [ ] 本番URLでの基本機能動作確認
- [ ] 本番データベースでの整合性確認
- [ ] 本番環境でのエラーログ確認

---

## 🔄 **Phase 6: ロールバック準備**

### **A. ロールバック可能性確認**
- [ ] 前回のコミット識別
- [ ] データベーススキーマ変更がある場合のマイグレーション逆実行計画
- [ ] ロールバック後の動作確認計画

### **B. 緊急時対応準備**
```bash
# ロールバック用コマンド準備
git log --oneline -10  # 最近のコミット確認
git show HEAD  # 現在のコミット詳細確認

# 必要に応じて：
# git revert HEAD  # 直前のコミットを取り消す新しいコミット作成
# または
# git reset --hard COMMIT_HASH  # 特定のコミットまで戻る（注意：データ消失リスク）
```

---

## ✅ **Phase 7: 最終確認・デプロイ実行**

### **A. デプロイ直前チェックリスト**
- [ ] 上記Phase 1-6の全チェック項目が完了
- [ ] デプロイ対象ブランチの確認（通常は`main`）
- [ ] コミットメッセージの適切性確認
- [ ] `.env`設定の本番環境向け確認

### **B. デプロイ実行**
```bash
# 最終コミット（すべてのファイルが適切にコミット済みであることを確認）
git add .
git status  # ステージングエリアの確認

# コミット（具体的で分かりやすいメッセージ）
git commit -m "fix: ナレッジカードID整合性・コース完了システム修正完了

- 修正内容: カードID生成とコレクション表示の整合性確保
- 修正内容: コース完了判定をセッション数ベースに修正  
- 修正内容: データベーススキーマエラー修正
- テスト: XP/SKP計算、ナレッジカード、コース完了の全機能確認済み
- 品質: TypeScript・ESLint・Build全てエラー0確認済み

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>"

# 本番プッシュ
git push origin main
```

### **C. デプロイ後即座確認（5分以内）**
- [ ] 本番サイトにアクセス可能
- [ ] ログイン機能正常動作
- [ ] 基本的なナビゲーション動作
- [ ] APIが正常にレスポンスを返す
- [ ] エラーログにクリティカルなエラーがない

### **D. デプロイ後完全確認（30分以内）**
- [ ] XP/SKP計算機能の動作確認
- [ ] ナレッジカード獲得・表示確認
- [ ] コース完了機能確認
- [ ] 新規ユーザー登録・ログイン確認
- [ ] データベースへの正常な書き込み確認

---

## 🚨 **緊急時エスカレーション**

### **デプロイ後に問題が発見された場合**
1. **即座にロールバック実行** （5分以内）
2. **問題の影響範囲特定** （10分以内）
3. **ユーザーへの影響評価** （15分以内）
4. **修正計画策定** （30分以内）

### **ロールバック判定基準**
以下のいずれかが発生した場合は即座にロールバック：
- XP/SKP計算が正常動作しない
- ナレッジカード獲得・表示ができない
- ユーザー認証ができない
- データベース書き込みエラーが発生
- サイトにアクセスできない

---

## 📊 **Phase 8: デプロイ後監視（24時間継続）**

### **A. 自動監視項目**
- [ ] サーバーレスポンス時間（3秒以内維持）
- [ ] エラー率（1%以下維持）
- [ ] データベース接続状況
- [ ] 新規ユーザー登録成功率

### **B. 手動確認項目（1時間おき）**
- [ ] 主要機能の正常動作確認
- [ ] エラーログの詳細確認
- [ ] ユーザーからの問い合わせ有無
- [ ] データベースパフォーマンス確認

---

## 🎯 **チェックリスト完了確認**

**最終確認者**: _____________  
**確認日時**: _____________  
**全チェック項目完了**: [ ] YES / [ ] NO  

**デプロイ実行許可**: [ ] 許可 / [ ] 不許可  
**許可理由/不許可理由**: ___________________________

---

**重要**: このチェックリストの項目を一つでも省略した場合、過去と同様のデプロイ問題が再発するリスクがあります。必ず全項目を実行してからデプロイを実行してください。

**最終更新**: 2025年10月14日  
**適用対象**: AI学習プラットフォーム全機能  
**次回見直し**: 重大問題発生時または1ヶ月後