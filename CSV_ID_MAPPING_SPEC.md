# CSV-DB ID マッピング仕様（絶対遵守）

**最終更新**: 2025年11月5日

## 🚨 **絶対に守るべきマッピング仕様**

### **1. 既存問題の更新時**
```typescript
// ✅ 正しい処理
CSV.id → DB.id (主キー) // 完全一致させる
CSV.legacy_id → DB.legacy_id // CSVの値をそのままセット（書き換えがあるため）
```

### **2. 新規問題の追加時**
```typescript
// ✅ 正しい処理
CSV.id = undefined/blank // CSVのidはブランクのまま渡す
DB.id = 自動生成 // データベースで自動採番
CSV.legacy_id → DB.legacy_id // CSVの値をそのままセット
```

## ❌ **絶対にやってはいけないこと**

```typescript
// ❌ 間違った処理（二度とやらない）
legacy_id: q.legacy_id || q.id! // idをlegacy_idにセットするのは間違い
legacy_id: q.legacy_id || 0 // デフォルト値を勝手に設定するのは間違い
```

## 📋 **理由**

1. **legacy_id**: CSVで書き換えることがあるため、CSVの値をそのまま保持する必要がある
2. **id（主キー）**: 既存問題では一意性を保つため、新規問題では自動生成で重複を防ぐ
3. **データ整合性**: 勝手な値の変更はデータ破損の原因となる

---
**注意**: この仕様に反する修正は絶対に行わない。TypeScriptエラーが出ても、この仕様に従って解決する。