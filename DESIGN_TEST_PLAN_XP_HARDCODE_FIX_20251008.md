# テスト計画書: XP/SKPハードコード修正

**作成日**: 2025年10月8日  
**機能**: XP/SKP計算APIのハードコード排除・テーブル参照統一化  
**重要度**: 🚨 **最高** - データ整合性・システム設定変更可能性に直結

---

## 📊 **影響範囲分析結果**

### **修正対象**
1. **🔴 最重要**: `app/api/xp-save/quiz/route.ts:196`
   - 現在: `calculateQuizXPUnified(1, 1, questionDifficulty)` 
   - 問題: `lib/xp-level-system.ts` の固定値使用
   - 影響: `quiz_answers.earned_xp` が一律10

2. **🔴 最重要**: `app/api/xp-save/course/route.ts:172`
   - 現在: `calculateCourseXPUnified(1)`
   - 問題: `lib/xp-level-system.ts` の固定値使用  
   - 影響: `course_session_completions` 記録が一律20

### **呼び出し元**
- **QuizSessionコンポーネント** → quiz API → XP計算
- **Course学習画面** → course API → XP計算
- **XP統計更新システム** ← 両API結果に依存

### **影響するコードパス**
1. **正常パス**: 問題回答 → XP計算 → DB記録 → 統計更新
2. **エラーパス**: XP計算失敗時のフォールバック
3. **ボーナスパス**: 高得点時の追加XP計算

---

## 🧪 **必須テストケース**

### **1. 全コードパステスト**

#### **A. Quiz API テスト**
- [ ] **基礎難易度**: `difficulty='basic'` → XP=10（テーブル値）
- [ ] **中級難易度**: `difficulty='intermediate'` → XP=20（テーブル値）
- [ ] **上級難易度**: `difficulty='advanced'` → XP=30（テーブル値）
- [ ] **エキスパート**: `difficulty='expert'` → XP=50（テーブル値）
- [ ] **不正難易度**: `difficulty='invalid'` → フォールバック動作

#### **B. Course API テスト**
- [ ] **基礎コース**: コース完了 → XP=15（テーブル値）
- [ ] **中級コース**: コース完了 → XP=25（テーブル値）
- [ ] **上級コース**: コース完了 → XP=35（テーブル値）
- [ ] **エキスパート**: コース完了 → XP=55（テーブル値）

#### **C. フォールバック動作テスト**
- [ ] **DB接続失敗**: テーブル読み込み不可 → デフォルト値使用
- [ ] **設定値不正**: 負の値・NULL → 安全なフォールバック
- [ ] **設定欠落**: 一部設定なし → 部分的フォールバック

### **2. 統合機能テスト**

#### **A. XP/SKP計算システム**
- [ ] **Quiz完了**: XP計算 → SKP計算 → 統計更新の連動
- [ ] **Course完了**: XP計算 → ボーナス → 統計更新の連動  
- [ ] **連続学習**: 複数セッション → 累積計算正確性

#### **B. データベース更新テスト**  
- [ ] **quiz_answers**: `earned_xp` が正確な値で記録
- [ ] **user_xp_stats_v2**: 総XP更新が正確
- [ ] **user_category_xp_stats_v2**: カテゴリ別XP更新
- [ ] **daily_xp_records**: 日別記録更新

#### **C. 統計情報更新テスト**
- [ ] **レベル計算**: XP増加 → レベルアップ判定
- [ ] **プログレス**: カテゴリー進捗の正確な反映
- [ ] **ランキング**: ユーザー間比較の整合性

### **3. エラーハンドリングテスト**

#### **A. API エラー時の処理**
- [ ] **DB接続エラー**: 適切なエラーメッセージ・ログ
- [ ] **設定読み込み失敗**: フォールバック動作・警告ログ
- [ ] **型変換エラー**: 不正データ処理・安全停止

#### **B. データ整合性エラー**
- [ ] **重複処理**: 同一セッション複数回実行防止
- [ ] **競合状態**: 並行アクセス時の整合性
- [ ] **ロールバック**: 部分更新失敗時の全体巻き戻し

---

## 🔧 **実装後チェックリスト**

### **コード品質**
- [ ] **TypeScript エラー**: 0個必須
- [ ] **ESLint エラー**: 0個必須  
- [ ] **ビルド成功**: 本番ビルド完全通過

### **機能動作確認**
- [ ] **開発環境**: 全テストケース実行完了
- [ ] **本番相当環境**: 実データでの動作確認
- [ ] **パフォーマンス**: 応答時間変化なし

### **システム監視**
- [ ] **アラート設定**: 異常値検出システム動作
- [ ] **ログ確認**: 正常動作・エラー状況の記録
- [ ] **メトリクス**: システムヘルスチェック正常

### **ユーザー影響確認**
- [ ] **既存ユーザー**: 統計値に異常変化なし
- [ ] **新規学習**: 正確なXP/SKP付与
- [ ] **画面表示**: UI上の値表示正確性

---

## 🚨 **重要注意事項**

### **デグレ防止**
- **段階的修正**: 1つのAPIずつ修正・テスト
- **フォールバック確保**: 必ず安全な代替動作を用意
- **ロールバック準備**: 問題発生時の即座復旧手順

### **データ保護**
- **バックアップ**: 重要テーブルの事前バックアップ
- **モニタリング**: リアルタイム異常検出
- **影響最小化**: テスト環境での十分な検証

### **運用考慮**
- **設定変更**: 管理者による動的設定変更テスト
- **キャッシュ管理**: 設定変更時のキャッシュクリア
- **パフォーマンス**: 追加DB アクセスの影響測定

---

## 📈 **成功基準**

1. **✅ 機能要件**: テーブル値が正確にXP計算に反映
2. **✅ 性能要件**: 既存API応答時間の±10%以内
3. **✅ 品質要件**: TypeScript/ESLint エラー0、テスト通過率100%  
4. **✅ 運用要件**: 設定変更がリアルタイム反映、監視アラート正常動作

**この計画に沿って安全かつ確実に修正を実行する**